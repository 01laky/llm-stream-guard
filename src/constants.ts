/** Default replacement token for redacted secret/PII material. */
export const DEFAULT_REDACT_PLACEHOLDER = "[REDACTED]";

/** Rolling tail retained for cross-chunk secret detection in byte mode. */
export const BYTE_LOOKBACK_SIZE = 128;
